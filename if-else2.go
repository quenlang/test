package main

import (
	"fmt"
)

func main() {
	var cond int
	// if err := file.Chmod(0664); err !=nil {
	if cond = 5; cond > 10 {
		fmt.Println("grater than 10.")
	} else {
		fmt.Println("less than 10.")
	}
}
